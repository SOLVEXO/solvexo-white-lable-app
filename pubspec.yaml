name: book_store_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.5+6

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  get: ^4.7.3
  uuid: ^4.5.2
  shared_preferences: ^2.5.4
  permission_handler: ^12.0.1
  fluttertoast: ^9.0.0
  intl: ^0.20.2
  file_picker: ^10.3.7
  responsive_sizer: ^3.3.1
  flutter_rating_bar: ^4.0.1
  flutter_svg: ^2.2.3
  shimmer: ^3.0.0
  smooth_page_indicator: ^2.0.1
  image_picker: ^1.2.1
  dio: ^5.9.0
  dio_smart_retry: ^7.0.1
  http_parser: ^4.1.2
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  google_maps_flutter: ^2.14.0
  cached_network_image: ^3.4.1
  pin_code_fields: ^8.0.1
  google_sign_in: ^7.2.0
  audioplayers: ^6.6.0
  lottie: ^3.3.2
  connectivity_plus: ^7.1.0
  flutter_markdown_plus: ^1.0.7
  http: ^1.6.0
  flutter_dotenv: ^6.0.0
  flutter_local_notifications: ^20.1.0
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  mobile_scanner: ^7.2.0
  share_plus: ^12.0.2
  url_launcher: ^6.3.2
  socket_io_client: ^3.1.6
  flutter_stripe: ^12.0.2
  video_player: ^2.10.1
  chewie: ^1.13.1
  flutter_html: ^3.0.0
  confetti: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
  # Android 8.0+ adaptive icon — provide separate foreground/background
  # Uncomment and set paths once you have a proper 1024×1024 source:
  # adaptive_icon_foreground: "assets/images/logo_foreground.png"
  # adaptive_icon_background: "#FFFFFF"
  remove_alpha_ios: true       # iOS App Store requires no transparency
  min_sdk_android: 21
# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/app_fonts/
    - assets/sounds/
    - assets/html/
    - .env

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Inter
      fonts:
        - asset: assets/app_fonts/inter/Inter_Regular.ttf
          weight: 400
        - asset: assets/app_fonts/inter/Inter_Medium.ttf
          weight: 500
        - asset: assets/app_fonts/inter/Inter_SemiBold.ttf
          weight: 600
        - asset: assets/app_fonts/inter/Inter_Bold.ttf
          weight: 700
    - family: Fraunces
      fonts:
        - asset: assets/app_fonts/fraunces/Fraunces_Light.ttf
          weight: 300
        - asset: assets/app_fonts/fraunces/Fraunces_Regular.ttf
          weight: 400
        - asset: assets/app_fonts/fraunces/Fraunces_Italic.ttf
          weight: 400
          style: italic
        - asset: assets/app_fonts/fraunces/Fraunces_SemiBold.ttf
          weight: 600
        - asset: assets/app_fonts/fraunces/Fraunces_Bold.ttf
          weight: 700
    - family: Manrope
      fonts:
        - asset: assets/app_fonts/manrope/Manrope_Regular.ttf
          weight: 400
        - asset: assets/app_fonts/manrope/Manrope_Medium.ttf
          weight: 500
        - asset: assets/app_fonts/manrope/Manrope_SemiBold.ttf
          weight: 600
        - asset: assets/app_fonts/manrope/Manrope_Bold.ttf
          weight: 700
    - family: IBM Plex Mono
      fonts:
        - asset: assets/app_fonts/ibm_plex_mono/IBMPlexMono_Regular.ttf
          weight: 400
        - asset: assets/app_fonts/ibm_plex_mono/IBMPlexMono_Medium.ttf
          weight: 500
        - asset: assets/app_fonts/ibm_plex_mono/IBMPlexMono_SemiBold.ttf
          weight: 600
        - asset: assets/app_fonts/ibm_plex_mono/IBMPlexMono_Bold.ttf
          weight: 700
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
